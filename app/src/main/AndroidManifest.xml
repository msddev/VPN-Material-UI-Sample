<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mkdev.vpnnewdesign">

    <application
        android:name=".VpnApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VpnNewDesign">
        <activity
            android:name=".OtpCodeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OtpIdActivity"
            android:exported="true"/>
        <activity android:name=".ServerNameActivity" />
        <activity android:name=".DownloadQrActivity" />
        <activity android:name=".ConnectActivity" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>